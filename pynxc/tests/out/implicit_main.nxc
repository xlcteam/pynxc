int x= 42;
task main() {
    x = 42; 
    
    for (x=0; x<10; x+=1)  {
        NumOut(0, LCD_LINE1, x);
        
    }
    
    while (true)  {
        Wait(10);
        
    }
    
    if ((true == false))  {
        TextOut(0, LCD_LINE1, "there is something wrong");
        
    }
    
    OnFwd(OUT_AB, 100);
    
    Wait(1000);
    
    
}
